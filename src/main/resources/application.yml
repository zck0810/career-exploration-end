server:
  port: 8520

spring:
  profiles:
    active: test
    
---
spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/init-h2.sql
      data-locations: classpath:sql/data-h2.sql

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# JWT configuration
jwt:
  secret: careerExplorationSecretKey2024ForContentManagementSystemTest
  expiration: 86400000  # 24 hours in milliseconds
  header: Authorization
  prefix: Bearer 

logging:
  level:
    com.fangzai: DEBUG

---
spring:
  profiles: prod
  datasource:
    url: jdbc:mysql://82.156.137.114:3306/careerexploration?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: careerexploration
    password: zhaochangkai0810

  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 2000ms
    jedis:
      pool:
        max-active: 100
        max-idle: 10
        min-idle: 0
        max-wait: 2000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# JWT configuration
jwt:
  secret: careerExplorationSecretKey2024ForContentManagementSystem
  expiration: 86400000  # 24 hours in milliseconds
  header: Authorization
  prefix: Bearer 
